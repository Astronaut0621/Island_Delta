# Database

This directory is reserved for GaussDB/openGauss assets.

Planned files:

- `schema.sql`
- `seeds/seed_locations.sql`
- `seeds/seed_emotion_labels.sql`
- `seeds/seed_mock_emotions.sql`

The current skeleton does not create tables or seed data.
