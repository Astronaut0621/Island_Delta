[project]
name = "island-delta-nlp-service"
version = "0.1.0"
description = "FastAPI shell for Island Delta NLP and agent service"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.32.0",
  "pydantic>=2.9.0"
]

[tool.uvicorn]
factory = false
